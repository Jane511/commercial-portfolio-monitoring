# Notebook Guide

This folder is reserved for reviewer-facing walkthrough notebooks for the monitoring workflow.

Recommended notebook split:

- data staging and demo-input generation
- staging and lifetime-PD logic
- scenario weighting and ECL calculation
- migration, concentration, and disclosure outputs

The pipeline is fully runnable from `src/` and `scripts/`, so notebooks remain optional review aids rather than required execution paths.
