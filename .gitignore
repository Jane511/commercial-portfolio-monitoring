# --- Internal build spec / prompt (not part of the public project) ---
PROJECT_BUILD*.md

# --- Editor / local tooling ---
.claude/

# --- Python ---
__pycache__/
*.pyc
*.egg-info/
.ipynb_checkpoints/
.DS_Store

# --- Raw SBA data (large public FOIA CSVs + data dictionary) ---
# Download these yourself from data.sba.gov — see README "Data sources & provenance".
# Only small output snapshots, charts, and the report are committed (in outputs/).
data/input/
data/manual/
data/processed/

# --- Runtime logs ---
logs/

# --- Test scratch ---
tests/_artifacts/
tests/.tmp_pytest/

# NOTE: outputs/ (tables, charts, reports) is intentionally NOT ignored —
# those committed snapshots are what makes the repo self-demonstrating.
